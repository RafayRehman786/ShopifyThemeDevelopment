<h1>{{ section.settings.heading_text }}</h1>
{% style %}
  h1 {
    color: {{ section.settings.heading_color }};
  }
{% endstyle %}
{% schema %}

{

  "name": "Heading",
  "settings": [
    {
      "type": "text",
      "id": "heading_text",
      "label": "Heading Text",
      "default": "Hello world"
    },
    {
        "type": "color",
       "id": "heading_color",
        "label": "Heading Color",
        "default": "#000000"
    
    }
  ],
    "presets": [
        {
        "name": "Heading"
        }
    ]
}


{% endschema %}