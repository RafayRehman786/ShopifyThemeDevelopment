<h1>{{ section.settings.heading_text }}</h1>

{% schema %}

{

  "name": "Heading",
  "settings": [
    {
      "type": "text",
      "id": "heading_text",
      "label": "Heading Text",
      "default": "Hello world"
    },
    {
        "type": "color",
       "id": "heading_color",
        "label": "Heading Color",
        "default": "#000000"
    
    }
  ],
    "presets": [
        {
        "name": "Heading"
        }
    ]
}


{% endschema %}